# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

## Apple tastatur 2006
# 191 - 194: F13 - F16
# 169: Eject button

## Etc.
# 135 - Kontekstmenyknap
#
set $mod Mod4

# font for window titles. ISO 10646 = Unicode
font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
font pango:DejaVu Sans Mono, Font Awesome, Arial Unicode MS, Apple Symbols 10px

#font pango:Terminus 10px
#client.
#client.focused #FF00FF #AA00AA #FFFFFF
#client.focused_inactive #333333 #5F676A #EEEEEE

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

bindcode 192 exec mpc prev
bindcode 193 exec mpc toggle
bindcode 194 exec mpc next

bindcode $mod+192 exec scrot -b -e "beep -l 10 && ablaze -s \$f && beep -f 400 -l 100 -n -f 500 -l 100 && mv \$f /home/sl/Pictures/Screenshots/" -q 100
bindcode $mod+193 exec scrot -b -u -e "beep -l 10 && ablaze -s \$f && beep -f 400 -l 100 -n -f 500 -l 100 && mv \$f /home/sl/Pictures/Screenshots/" -q 100
bindcode $mod+Shift+192 exec scrot -b -e "beep -l 10 && ablaze -s -p \$f && beep -f 400 -l 100 -n -f 500 -l 100 && mv \$f /home/sl/Pictures/Screenshots/" -q 100
bindcode $mod+Shift+193 exec scrot -b -u -e "beep -l 10 && ablaze -s -p \$f && beep -f 400 -l 100 -n -f 500 -l 100 && mv \$f /home/sl/Pictures/Screenshots/" -q 100
#bindcode $mod+194 exec DISPLAY=:0.0 scrot -b -s -e "feh \$f"

bindsym $mod+g exec groundbreaker
bindsym $mod+shift+f exec pantheon-files
bindsym $mod+b exec chromium-browser
bindsym $mod+shift+d exec slingshot-launcher

bindsym $mod+t exec i3lock -i /home/sl/.groundbreaker/lock.png -t -p default -d -n
bindsym $mod+o exec xdg-open http://ablaze.web/post/`cat /home/sl/.groundbreaker/postid` &>2

bindcode $mod+Shift+61 exec touch /home/sl/.groundbreaker/run.lock && rm /home/sl/.groundbreaker/bg /home/sl/.groundbreaker/lock.png && cp /home/sl/.groundbreaker/black.png /home/sl/.groundbreaker/bg && cp /home/sl/.groundbreaker/black.png /home/sl/.groundbreaker/lock.png && feh --bg-scale /home/sl/.groundbreaker/bg
bindcode $mod+Shift+60 exec rm /home/sl/.groundbreaker/run.lock

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal

# kill focused window
bindsym $mod+Shift+W kill

# start dmenu (a program launcher)
bindsym $mod+d exec dmenu_run

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+oslash focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+J move left
bindsym $mod+Shift+K move down
bindsym $mod+Shift+L move up
bindsym $mod+Shift+Oslash move right

# alternatively, you can use the cursor keys:
bindsym $mod+m exec slingshot-launcher
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+shift+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, default)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout default

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

bindsym $mod+Shift+B border toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindcode $mod+d focus child

# switch to workspace
bindsym $mod+1 workspace 1
bindsym $mod+2 workspace 2
bindsym $mod+3 workspace 3
bindsym $mod+4 workspace 4
bindsym $mod+5 workspace 5
bindsym $mod+6 workspace 6
bindsym $mod+7 workspace 7
bindsym $mod+8 workspace 8
bindsym $mod+9 workspace 9
bindsym $mod+0 workspace 10
bindsym $mod+F1 workspace 11
bindsym $mod+F2 workspace 12
bindsym $mod+F3 workspace 13
bindsym $mod+F4 workspace 14
bindsym $mod+F5 workspace 15
bindsym $mod+F6 workspace 16
bindsym $mod+F7 workspace 17
bindsym $mod+F8 workspace 18
bindsym $mod+F9 workspace 19
bindsym $mod+F10 workspace 20
bindsym $mod+F11 workspace 21
bindsym $mod+F12 workspace 22

# move focused container to workspace
bindsym $mod+Shift+exclam move workspace 1
bindsym $mod+Shift+quotedbl move workspace 2
bindsym $mod+Shift+numbersign move workspace 3
bindsym $mod+Shift+currency move workspace 4
bindsym $mod+Shift+percent move workspace 5
bindsym $mod+Shift+ampersand move workspace 6
bindsym $mod+Shift+slash move workspace 7
bindsym $mod+Shift+parenleft move workspace 8
bindsym $mod+Shift+parenright move workspace 9
bindsym $mod+Shift+equal move workspace 10
bindsym $mod+Shift+F1 move workspace 11
bindsym $mod+Shift+F2 move workspace 12
bindsym $mod+Shift+F3 move workspace 13
bindsym $mod+Shift+F4 move workspace 14
bindsym $mod+Shift+F5 move workspace 15
bindsym $mod+Shift+F6 move workspace 16
bindsym $mod+Shift+F7 move workspace 17
bindsym $mod+Shift+F8 move workspace 18
bindsym $mod+Shift+F9 move workspace 19
bindsym $mod+Shift+F10 move workspace 20
bindsym $mod+Shift+F11 move workspace 21
bindsym $mod+Shift+F12 move workspace 22

#bindsym $mod+y exec curl "http://10.0.1.7:3000/xbmcCmds/xbmcHttp?command=Previous"
bindsym $mod+u exec curl "http://10.0.1.7:3000/xbmcCmds/xbmcHttp?command=Pause"
#bindsym $mod+i exec curl "http://10.0.1.7:3000/xbmcCmds/xbmcHttp?command=Next"

#bindsym $mod+u exec curl "http://10.0.1.7:3005/jsonrpc?request=%7B%22jsonrpc%22%3A%222.0%22%2C%22method%22%3A%22Player.PlayPause%22%2C%22params%22%3A%7B%22playerid%22%3A0%7D%2C%22id%22%3A%22ahue1%22%7D"

# reload the configuration file
bindsym $mod+Shift+C reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+R restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+E exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"
bindsym $mod+Shift+S exec scrot

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # They resize the border in the direction you pressed, e.g.
        # when pressing left, the window is resized so that it has
        # more space on its left

bindsym j resize shrink left 10 px or 10 ppt
bindsym Shift+J resize grow   left 10 px or 10 ppt

bindsym k resize shrink down 10 px or 10 ppt
bindsym Shift+K resize grow   down 10 px or 10 ppt

bindsym l resize shrink up 10 px or 10 ppt
bindsym Shift+L resize grow   up 10 px or 10 ppt

bindsym oslash resize shrink right 10 px or 10 ppt
bindsym Shift+Oslash resize grow   right 10 px or 10 ppt

        # same bindings, but for the arrow keys
bindsym Left resize shrink left 10 px or 10 ppt
bindsym Shift+Left resize grow   left 10 px or 10 ppt

bindsym Down resize shrink down 10 px or 10 ppt
bindsym Shift+Down resize grow   down 10 px or 10 ppt

bindsym Up resize shrink up 10 px or 10 ppt
bindsym Shift+Up resize grow   up 10 px or 10 ppt

bindsym Right resize shrink right 10 px or 10 ppt
bindsym Shift+Right resize grow   right 10 px or 10 ppt

        # back to normal: Enter or Escape
bindsym Return mode "default"
bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

mode "Games" {
    bindcode 60 workspace "Steam"; mode "default"
    bindsym s workspace "Stepmania"; mode "default"
    bindsym c workspace "CS:S"; mode "default"

    bindsym Return mode "default"
    bindsym Escape mode "default"
    bindcode 146 mode "default"
}

bindcode 121 exec pulseaudio-ctl mute
bindcode 122 exec pulseaudio-ctl down
bindcode 123 exec pulseaudio-ctl up

bindcode 146 mode "Games"

bindsym $mod+shift+g mode "Games"

mode "Display Configuration" {
    bindsym 1 exec exec xrandr --output LVDS1 --mode 1280x800; mode "default"
    bindsym 3 exec exec xrandr --output VGA1 --mode 1920x1200; mode "default"
    bindsym 2 exec exec xrandr --output LVDS1 --mode 1280x800; exec xrandr --output VGA1 --primary --mode 1920x1200 --left-of LVDS1; mode "default"
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+Shift+P mode "Display Configuration"

for_window [title="OpenGL Driver configuration"] floating enable
for_window [title="Properties"] floating enable
for_window [title="Properties"] border none
for_window [title="Slingshot"] floating enable
for_window [title="Slingshot"] border none
for_window [title="slingshot-launcher"] floating enable
for_window [title="slingshot-launcher"] border none
#for_window [title="exe"] fullscreen
#for_window [title="exe"] border none
#for_window [title="exe"] move workspace "Flash"
#for_window [title="exe"] workspace "Flash"

## Gaming organization binds
#for_window [class="Steam"] move workspace Steam
for_window [class="Steam" title="(Steam|Friends|.+Chat)"] border none
#for_window [class="Steam" title="[^(Steam|Friends|.+Chat)]"] floating enable

for_window [class="Conky"] border 1pixel; floating enable; resize grow height 100 px or 100 ppt

for_window [class="StepMania"] border none
for_window [class="StepMania"] fullscreen off
#for_window [class="StepMania"] move workspace "StepMania"

#for_window [class="dota_linux"] border none
#for_window [class="dota_linux"] fullscreen off
#for_window [class="dota_linux"] move workspace "Dota 2"

#for_window [class="hl2_linux" title="Counter.+"] border none
#for_window [class="hl2_linux" title="Counter.+"] fullscreen off
#for_window [class="hl2_linux" title="Counter.+"] move workspace "CS:S"
######
for_window [title=".+ - Oracle VM VirtualBox"] floating enable

set $mode_system System (l) lock, (e) logout, (s) suspend, (h) hibernate, (r) reboot, (Shift+s) shutdown
mode "$mode_system" {
    bindsym l exec --no-startup-id i3exit lock, mode "default"
    bindsym e exec --no-startup-id i3exit logout, mode "default"
    bindsym s exec --no-startup-id i3exit suspend, mode "default"
    bindsym h exec --no-startup-id i3exit hibernate, mode "default"
    bindsym r exec --no-startup-id i3exit reboot, mode "default"
    bindsym Shift+s exec --no-startup-id i3exit shutdown, mode "default"

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+Pause mode "$mode_system"

bindsym $mod+Shift+o exec i3-input

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
    status_command /home/sl/.i3/status.py
    colors {
        #focused_workspace #FF00FF #AA00AA #EEEEEE
    }
}
#exec compton -cCzfbi 0.8 --inactive-dim 0.2 --mark-wmwin-focused --mark-ovredir-focused --vsync opengl --blur-background --dbus
#exec nm-applet &
#exec pulseaudio &
